---
- name: playbook run command
  hosts: ec2
  become: yes
  tasks:
    - name: run command
      command: cat /etc/hostname
      register: out_hostname
    - debug:
        var=out_hostname
